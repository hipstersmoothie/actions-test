name: pr-stopship

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
            fetch-depth: 0

                  
      - name: Test
        run: |
          git log;
          git tag;
